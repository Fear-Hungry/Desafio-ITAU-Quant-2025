"""Configura logging estruturado do projeto.

Implementar funcao configure_logging() que gera logs JSON em stdout,
define niveis por modulo, inclui contexto (timestamp, seed, etapa) e permite
customizacao via Settings.
"""
